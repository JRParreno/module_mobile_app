export default class Amun {
    pk: string;
    title: string;
    path: string;

  constructor(pk: string, title: string, path: string) {
    this.pk = pk
    this.title = title
    this.path = path
  }

}